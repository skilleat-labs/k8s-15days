apiVersion: v1
kind: Pod
metadata:
  name: shop-prod-pod
spec:
  containers:
  - name: shop
    image: busybox
    command: ["sh", "-c", "env && sleep 3600"]
    envFrom:
    - configMapRef:
        name: shop-prod-config
    - secretRef:
        name: shop-prod-secret
